# Deployment note

The reviewer-oriented source changes require the hosting service to redeploy the repository revision before they appear in the public Streamlit app. Repository source state and deployed-app state should be verified separately.
